# docs/specs/

Un SPEC.md por feature grande (no por ticket chico — eso vive en `tickets/<ID>/plan.md`). En este proyecto los tres tickets de práctica (TK-101/102/103) son chicos y su spec vive completa en su propio `plan.md` — todavía no hay ninguna feature lo bastante grande como para justificar un spec acá.
