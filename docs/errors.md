# docs/errors.md (movido a `docs/handbook/errors.md`)

> **Este archivo se movió.** El catálogo de errores conocidos ahora vive en `docs/handbook/errors.md`, junto con el resto del contenido operativo que se carga bajo demanda (`docs/handbook/`). Ver `HANDBOOK.md` en la raíz para el índice completo.
